// import { create } from "zustand";

// export const useFavoriteStore = create((set) => ({
//   favoriteCount: 0,
//   addToFavorite: () =>
//     set((state) => ({ favoriteCount: state.favoriteCount + 1 })),
//   removeFromFavorite: () =>
//     set((state) => ({
//       favoriteCount: state.favoriteCount > 0 ? state.favoriteCount - 1 : 0,
//     })),
// }));
